version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: dev.web.Dockerfile
    container_name: dan-dan-web
    ports:
      - "5173:5173"
    volumes:
      - ./web:/web
    working_dir: /web
    stdin_open: true
    tty: true
    command: sh -c "npm install && npm run dev"
